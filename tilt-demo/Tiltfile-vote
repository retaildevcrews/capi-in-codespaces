# pyright: reportUndefinedVariable=false

voteAppURL = os.environ.get('VOTE_APP_URL', '')

docker_compose('./docker-compose.yml')

dc_resource('vote',
            links=[
                link(voteAppURL, 'Vote app (codespace)')
            ],
            labels=['vote'])

dc_resource('redis',
            labels=['vote'])
